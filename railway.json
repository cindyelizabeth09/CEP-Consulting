{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "pnpm build && PORT=${PORT:-3000} pnpm preview",
    "restartPolicyMaxRetries": 5
  }
}
